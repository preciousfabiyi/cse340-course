<%- include('partials/header') %>

<main>
  <h1><%= title %></h1>

  <% if (typeof errors !== 'undefined' && errors) { %>
    <ul class="notice">
      <% errors.forEach(error => { %>
        <li><%= error %></li>
      <% }) %>
    </ul>
  <% } %>

  <form action="/new-project" method="POST">
    <label for="organization_id">Organization</label>

    <select id="organization_id" name="organization_id" required>
      <option value="">Select an organization</option>

      <% organizations.forEach(organization => { %>
        <option
          value="<%= organization.organization_id %>"
          <%= typeof project !== 'undefined' &&
              String(project.organization_id) === String(organization.organization_id)
              ? 'selected'
              : '' %>
        >
          <%= organization.name %>
        </option>
      <% }) %>
    </select>

    <label for="title">Project Title</label>

    <input
      type="text"
      id="title"
      name="title"
      value="<%= typeof project !== 'undefined' ? project.title : '' %>"
      maxlength="150"
      required
    />

    <label for="description">Description</label>

    <textarea
      id="description"
      name="description"
      required
    ><%= typeof project !== 'undefined' ? project.description : '' %></textarea>

    <label for="location">Location</label>

    <input
      type="text"
      id="location"
      name="location"
      value="<%= typeof project !== 'undefined' ? project.location : '' %>"
      maxlength="200"
      required
    />

    <label for="date">Date</label>

    <input
      type="date"
      id="date"
      name="date"
      value="<%= typeof project !== 'undefined' ? project.date : '' %>"
      required
    />

    <button type="submit">Create Service Project</button>
  </form>
</main>

<%- include('partials/footer') %>